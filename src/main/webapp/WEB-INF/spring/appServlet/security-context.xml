<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<!-- @을 위한 component-scan --><!-- 
	<context:component-scan
		base-package="project.suhbuway.security,project.suhbuway.dao,project.suhbuway.service" /> -->
		
	<context:component-scan base-package="project.suhbuway" />

	<!-- passwordEncoder : DB암호문과 입력평문값을 비교 -->
	<bean id="passwordEncoder"
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"></bean>
		
	<!-- 로그인 실패시 tiles 적용 위한 bean 등록 -->
	<bean id="memberAuthenticationFailHandler" class="project.suhbuway.security.MemberAuthenticationFailHandler">
	</bean>
	

	<!-- 인증권한 체크 -->
	<security:http auto-config="true" use-expressions="true">
		<security:intercept-url pattern="user/main" access="isAuthenticated()" />
		<security:intercept-url pattern="/admin/main" access="hasRole('ROLE_ADMIN')" />

		<!-- 예약어/ 직접 매핑 /j_spring_security_check -->
		<security:form-login 
			login-page="/user/login"
			login-processing-url="/user/userLogin"   
			username-parameter="id"
			password-parameter="password" 
			default-target-url="/"
			authentication-failure-handler-ref="memberAuthenticationFailHandler" /> <!-- authentication-failure-url="/user/loginForm?ng" -->

		<security:logout
			logout-url="/user/logout"
			invalidate-session="true" 
			delete-cookies="JSESSIONID"
			logout-success-url="/" />
	</security:http>
	
	
	<!-- 계정에 따른 권한을 설정 - DB 연동( 직접 Provider 구현해서 생성 ) -->
	<security:authentication-manager>
		<security:authentication-provider 
			ref="memberAuthenticationProvider">
		</security:authentication-provider>	
	</security:authentication-manager>
	
</beans>
